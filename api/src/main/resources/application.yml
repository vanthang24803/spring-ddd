server:
  port: 8000

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/maria
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: Thang@240803
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
